"""
순차적 자막 기능 사용법 및 설정 가이드
"""

# 🎯 주요 개선사항

## 1. 순차적 자막 생성 기능 추가
- `create_sequential_subtitle_file()` 함수 추가
- 한 줄씩 순차적으로 나타나는 자막 생성
- 각 줄이 완전히 끝나고 간격을 두고 다음 줄 시작

## 2. 개선된 자막 스타일
- `get_sequential_subtitle_style()` 함수 추가
- 더 두꺼운 외곽선과 그림자 효과
- 가독성 향상을 위한 최적화된 스타일

## 3. 자막 처리 파라미터 설정
```python
# 기본 설정
max_chars = 15          # 한 줄당 최대 문자 수
line_duration = 1.5     # 각 줄 표시 시간 (초)
gap_duration = 0.3      # 줄 사이 간격 (초)
font_size = 18          # 폰트 크기
```

## 4. 사용법

### 기본 사용
```python
from subtitle_utils import merge_video_with_tts_and_subtitles

result = await merge_video_with_tts_and_subtitles(
    video_urls=["영상URL"],
    tts_scripts=["음성으로 변환할 텍스트"],
    voice_id="Xb7hH8MSUJpSbSDYk0k2",
    add_subtitles=True,  # 자막 활성화
    enable_bgm=True,
    tts_volume=0.8,
    video_volume=0.3
)
```

### 자막만 생성
```python
from subtitle_utils import create_sequential_subtitle_file

sequential_subtitle_path = create_sequential_subtitle_file(
    subtitle_file_path="원본.srt",
    output_path="순차적_자막.srt",
    max_chars=15,
    line_duration=1.5,
    gap_duration=0.3
)
```

## 5. 자막 시간 배치 방식

### 기존 방식 (문제 있음)
```
00:00:00 - 00:00:02 "안녕하세요 여러분"
00:00:02 - 00:00:04 "오늘은 특별한 날"  (겹침 가능)
```

### 새로운 순차적 방식
```
00:00:00 - 00:00:01.5 "안녕하세요 여러분"
00:00:01.8 - 00:00:03.3 "오늘은 특별한 날"
00:00:03.6 - 00:00:05.1 "함께 시작해봅시다"
```

## 6. 설정 조정 가능한 값들
- `max_chars`: 줄당 문자 수 (10-20 권장)
- `line_duration`: 표시 시간 (1.0-2.0초 권장)
- `gap_duration`: 간격 시간 (0.2-0.5초 권장)
- `font_size`: 폰트 크기 (16-24 권장)

## 7. 테스트 파일
- `test_sequential_subtitles.py`: 자막 기능 단독 테스트
- `test_video_sequential_subtitles.py`: 전체 영상 제작 테스트

이제 영상에서 텍스트가 잘리지 않고 한 줄씩 깔끔하게 나타날 것입니다! 🎉
